import React from "react";

export const LLMNodeContent = ({ id, data, onDataChange, nodeType }) => {
  return (
    <div>
      <span style={{ fontSize: "13px", color: "#FFFFFF" }}>
        Large Language Model
      </span>
    </div>
  );
};
